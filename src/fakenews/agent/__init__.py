"""Agentic layer: a deterministic finite-state machine
(parse → classify → check-worthiness → retrieve → stance → verdict → present)
with ≥4 decision points and an optional, advisory LLM brain that falls back to rules."""
