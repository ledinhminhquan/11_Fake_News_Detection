# Full local/dev install (everything). For a minimal core install use `pip install -e .`
# and add extras: `.[ml]`, `.[api]`, `.[report]`, or `.[all]`.

# ── core (package + TF-IDF/LogReg classifier + BM25 retrieval + lexical stance + metrics + tests) ──
numpy>=1.23
pyyaml>=6.0
pydantic>=2.0
scikit-learn>=1.2
joblib>=1.3

# ── trainable transformer classifier + stance/NLI + dense evidence retrieval ──
torch>=2.1
transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentence-transformers>=3.0

# ── serving (API + UI) ──────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0

# ── reporting (auto report.pdf + slides.pptx) ───────────────────────────────
reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7

# ── optional ────────────────────────────────────────────────────────────────
# anthropic>=0.39          # optional LLM fact-check rationale brain for the agent
