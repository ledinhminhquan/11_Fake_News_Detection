# Colab-safe dependencies. IMPORTANT: do NOT list torch here — Colab ships a CUDA
# build already, and reinstalling it breaks the runtime. The notebook installs
# these, then `pip install -e . --no-deps`.

transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentence-transformers>=3.0
scikit-learn>=1.2
joblib>=1.3

fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0

reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7
